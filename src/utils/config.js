const testnetConfig = {
  // Base_URL: "https://myapi.brandsearchengine.com/",
  Base_URL: "http://192.168.100.163:8000/",
};
const mainnetConfig = {
  Base_URL: "",
};

const stagingConfig = {
  Base_URL: "",
};

const defaultConfig = testnetConfig;

export default defaultConfig;